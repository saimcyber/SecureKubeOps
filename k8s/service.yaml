apiVersion: v1
kind: Service
metadata:
  name: securekubeops-service

spec:
  type: NodePort

  selector:
    app: securekubeops

  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30007